{
  "name": "dockersample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client": "cd client && cd library && yarn start",
    "server": "nodemon index.js",
    "dev": "concurrently --kill-others-on-fail \"nodemon index\" \"yarn client\"",
    "dev:server": "cd client &&  cd library && yarn build && cd .. && yarn start",
    "start": "nodemon index.js"
  },
  "author": "Hamza",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.4",
    "body-parser": "*",
    "express": "^4.16.4",
    "jsonwebtoken": "^8.5.0",
    "mongoose": "*",
    "react-image-magnifier-lens": "^1.1.9"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
